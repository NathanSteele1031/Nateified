#include "inventory.h"
#include <stdlib.h>
#include <time.h>

#ifndef WILDERNESS_H
#define WILDERNESS_H

Item* scavange(); // This will be an arrary, the first input will tell how many items are in the arrary minus the first value
int getRandomValue(int min, int max);

#endif